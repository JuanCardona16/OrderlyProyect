import { Container } from "../../../components/UI";
import styles from "./settings.module.css";

const Settings = () => {
  return (
    <Container>
      <h2>Configuración</h2>
    </Container>
  );
};

export default Settings;
